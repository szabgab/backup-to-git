import backup

# create a temporary directory for the source files
# create a temp directory for the git repository
# run the backup
# check the results
#backup.run()
def test_backup():
    assert True
